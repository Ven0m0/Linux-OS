name: Shell Lint & Format
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "**/*.{sh,bash,bashrc,profile}"
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
  pull-requests: write
env:
  DEBIAN_FRONTEND: noninteractive
  LC_ALL: C
  GITHUB_TOKEN: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}
  SHELLCHECK_OPTS: "--source-path=SCRIPTDIR --external-sources --check-sourced --severity=style --shell=bash"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
      - name: Install tools
        run: |
          sudo apt-get update -y && sudo apt-get install -y shellcheck shfmt
          curl -sfL https://github.com/anordal/shellharden/releases/latest/download/shellharden-x86_64-unknown-linux-musl.tar.gz | tar xz
          sudo mv shellharden /usr/local/bin/
      - name: Find & fix shell files
        shell: bash
        run: |
          awk_fix='
            /^#[[:space:]]*-{5,}/ { next }
            {
              gsub(/\(\) \{/, "(){")
              gsub(/\|\| true/, "|| :")
              if (NF) { if (blank) print ""; blank=0; print }
              else blank=1
            }'
          while IFS= read -r f; do
            awk "$awk_fix" "$f" > "$f.tmp" && mv "$f.tmp" "$f"
            shellharden --replace "$f" &>/dev/null || :
            shellcheck -f diff "$f" | patch -Np1 &>/dev/null || :
            shfmt -i 2 -s -ln bash -w "$f" &>/dev/null || :
          done < <(
            {
              find -O2 . -type f \( -name '*.sh' -o -name '*.bash' -o -name '*bashrc' -o -name '*profile' \) -print 2>/dev/null || :
              find -O2 . -type f -exec grep -Il '^#!/.*sh' {} + 2>/dev/null || :
            } | sort -u
          )
      - name: Commit changes
        run: |
          git config user.name github-actions[bot] && git config user.email github-actions[bot]@users.noreply.github.com
          git diff --quiet || { git add -A && git commit -m "🔧 Auto-fix shell scripts" && git push; }
