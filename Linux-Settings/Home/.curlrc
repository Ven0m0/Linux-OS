### General ###
# silent but show errors
-sS
# follow redirects
-L
# resume partial downloads
-C -

# Use strongest modern TLS only
--tlsv1.2
#--tlsv1.3
--tls-max 1.3

# HTTP versions
#--http2
--http3

# Compression & HSTS
--compressed
--compressed-ssh
# --hsts

# Parallel
-Z
--parallel-max 16
# --parallel-max 8

# Network performance
--tcp-fastopen
--tcp-nodelay
--ipv4

# Create dirs for output
--create-dirs

# Not always supported
--tls-earlydata
--mptcp

# When following a redirect, automatically set the previous URL as referer.
--referer ";auto"

# Wait before timing out.
connect-timeout = 30

# Dont waste time on error
--fail-early
